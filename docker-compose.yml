version: '3.7'

services:
  app:
    container_name: app
    build: .
    command: bash -c "gunicorn -b 0.0.0.0:8000 boilerplate.wsgi"
    ports:
      - "8000:8000"
    volumes:
      - ./boilerplate:/app
